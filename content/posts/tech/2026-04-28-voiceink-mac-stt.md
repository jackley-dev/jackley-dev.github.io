+++
title = "Mac 语音输入方案：VoiceInk 体验备忘录"
date = 2026-04-28T00:00:00+08:00
slug = "voiceink-mac-stt"
categories = ["tech"]
tags = ["mac", "stt", "tools", "voiceink"]
+++

## 缺失的 Mac 语音交互
大语言模型（LLM）让“聊天”成为高频工作方式，语音输入需求暴增。

手机 App（如豆包、通义千问）自带完善的语音输入，但 Mac 端却是个痛点：系统自带的听写准确率差，第三方软件要么收费贵，要么响应慢。

## 踩坑与选择——VoiceInk
前前后后试用了几款产品，最后发现还是 **VoiceInk** 最能满足我的日常需求。作为一款开箱即用的 Mac 原生工具，它打动我的核心点主要有三个：

- **完全免费**：开源无内购。
- **中文精准**：语音识别准确率极高，自动添加标点，懂中文语境。
- **原生丝滑**：支持全局快捷键，光标在哪文字就实时输出在哪，无需复制粘贴。

## 极速安装
直接通过 Homebrew 一键搞定：
```bash
brew install --cask voiceink
```

## 核心配置
安装完成后，强烈建议进行以下基础配置，以兼顾识别性能与速度：

1. **模型选择**：推荐下载并使用 `large v3 turbo` 模型，实测在准确率和响应速度上取得了极佳的平衡。
2. **语言设置**：将转换语言显式设置为**中文**，避免中英文混杂时发生识别漂移。

![VoiceInk 核心配置](/images/voiceink-settings.jpg)

## 个人使用体验
默认快捷键（右侧 `⌘` 键）非常顺手。现在我跟 AI 聊天时，光标一点，按下快捷键直接“动嘴”，输入效率拉满。
