baseURL = "https://jackley-dev.github.io/"
languageCode = "zh-cn"
title = "我的博客"
theme = "PaperMod"

[params]
  defaultTheme = "auto"

  # ❌ 删除原来的 [params.profileMode] 相关设置
  # ✅ 改用下面的 [params.homeInfoParams]

  [params.homeInfoParams]
    Title = "Hi there 👋"
    # Content 支持 Markdown 语法，可以换行
    Content = """
    欢迎来到我的blog，这里记录我的技术成长、踩坑经历与生活思考。
    """

[permalinks]
  # 保持 URL 结构为 /posts/filename/，不受子目录分类影响
  # 这样你可以随意调整 content/posts 下的文件夹结构，而不会破坏现有的 URL
  posts = "/posts/:slug/"

# 顶部菜单 (保持不变)
[menu]
  [[menu.main]]
    identifier = "life"
    name = "Life"
    url = "/categories/life/"
    weight = 10

  [[menu.main]]
    identifier = "tech"
    name = "Tech"
    url = "/categories/tech/"
    weight = 20

  # --- 新增 GitHub 入口 ---
  [[menu.main]]
    identifier = "github"
    name = "GitHub"       # 你也可以用 emoji 比如 "🐙 GitHub"
    url = "https://github.com/jackley-dev" # 记得换成你的真实地址
    weight = 30           # 数字越大，越靠右

    [menu.main.params]
      # 这是一个小技巧：告诉浏览器在新标签页打开
      external = true
